# ignorer le dossier de config de visual studio code
.vscode/

# ignorer les dossier tex/, tex/png/, dnb/ et tous leurs enfants
# tex/*
# tex/png/*
dnb/

#ignorer le fichier de config de jshintrc
.jshintrc

# ignorer les zip du dossier fichierszip/
# plus utile puisque ça se passe à partir du build
# à confirmer, une fois confirmé à supprimer
fichierszip/*.zip
!/fichierszip/Projet.zip
!/fichierszip/ProjetCrashed.zip

#ignorer le fichier src/json/scoresCodesVip.json utile pour générer les espaces de scores pour les V.I.P.
src/json/scoresCodesVip.json


# les dépendances
node_modules
# le build
build
# les logs
log
# la doc
documentation
src/js/modules/doc
# la conf privée
_private

# Par défaut, on utilise pnpm.
# On enlève ce fichier pour ceux qui utilisent npm
package-lock.json


# traces MacOS
.DS_Store
src/json/scoresCodesVip.json
src/json/scoresCodesVipSebSeul.json
