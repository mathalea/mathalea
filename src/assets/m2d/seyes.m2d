fenetreMathalea2d = [-2,-15,28,5]
pixelsParCm = 40 // L'unité fait 40 pixels
seyes(0,0,10,4) // Rectangle de carreaux Seyes avec xmin = ymin = 0, xmax = 10, ymax = 4
let A = point(1,1,'A') // Créé un point A qui a pour nom...'A'
let B = point(5,2,'B')
let s = segmentAvecExtremites(A,B) // Créé le segment en dessinant les 2 extrémités
labelPoint(A,B) // Ajoute le nom des points
texteSurSegment(nombreDecimal(longueur(A,B)*0.8,1)+' cm',A,B)
//nombreDecimal permet d'avoir la virgule comme séparateur décimal et s'assure de la précision des calculs avec des décimaux.
// Ici, on multiplie par 0,8 car c'est la longueur du côté d'un vrai carreau