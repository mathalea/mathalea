allerA(600,220)
context.lutin.color = colorToLatexOrHTML('blue') // Change la couleur
baisseCrayon()
for (let a = 0 ; a < 6 ; a++) { // Répète 6 fois 
    for (let i = 0 ; i < 4 ; i++){ // Trace le carré
    avance(200)
    tournerG(90)
  }
  tournerG(60) // Tourne de 60° vers la gauche
}